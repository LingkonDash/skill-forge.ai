/**
 * Better Auth client-side config — Phase 2 implements this fully.
 * Shell created in Phase 0 to satisfy folder-structure.md.
 *
 * This is the CLIENT-SIDE Better Auth config (React hooks, session state).
 * Distinct from lib/domains/auth/auth.service.ts (server-side config).
 */

// TODO (Phase 2): implement Better Auth client SDK + session hooks
export {};
