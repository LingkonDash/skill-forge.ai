/**
 * Coach agent — Phase 9 implements this fully.
 * Shell created in Phase 0 to satisfy folder-structure.md.
 *
 * SERVER-ONLY: must never be imported from a "use client" component.
 */

// TODO (Phase 9): implement streamCoachResponse() per ai-pipeline.md §3
export {};
