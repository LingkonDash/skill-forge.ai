/**
 * Shared TypeScript types — populated as domains are implemented.
 *
 * These are request/response DTOs used by both route handlers and
 * frontend components, matching api-contract.md field-for-field.
 * Per folder-structure.md §5 and coding-guidelines.md §1.
 */

// Types are added per phase alongside the domain they belong to.
export {};
